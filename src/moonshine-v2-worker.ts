// The served implementation lives in app/moonshine-v2-worker.js. It is kept
// as plain JavaScript because Innercast deliberately has no build dependency.
// See that file for the direct ONNX Runtime Moonshine v2 inference pipeline.
export {};
